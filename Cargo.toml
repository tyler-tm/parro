[package]
name = "parro"
version = "0.1.0"
edition = "2024"

[features]
default = ["server"]
client = []
server = []

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
bincode = "1"
